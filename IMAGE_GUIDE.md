# 🖼️ 图片替换指南

## 当前状态

网站所有图片使用 `placehold.co` 占位图服务，需要替换为真实产品图片。

## 📁 图片目录结构

```
public/images/
├── products/          # 产品图片
│   ├── sports-car.jpg
│   ├── motorcycle.jpg
│   ├── tractor.jpg
│   ├── jeep.jpg
│   ├── police-car.jpg
│   ├── bus.jpg
│   ├── racing-cart.jpg
│   ├── fire-truck.jpg
│   └── scooter.jpg
├── blog/              # 博客特色图片
│   ├── safe-toys.jpg
│   ├── child-development.jpg
│   └── eco-friendly.jpg
└── hero/              # 首页大图
    └── happy-kid.jpg
```

## 🔄 替换步骤

### 1. 准备图片

准备以下规格的图片：
- **产品图片**：建议尺寸 800x600 或 600x400
- **博客图片**：建议尺寸 1200x600
- **首页大图**：建议尺寸 1600x900

### 2. 添加图片到项目

将图片复制到 `public/images/` 对应的子目录中。

### 3. 更新代码中的图片路径

#### 更新产品页面 (`src/app/products/page.tsx`)

将：
```typescript
imageSrc: 'https://placehold.co/600x400?text=Sports+Car',
```

改为：
```typescript
imageSrc: '/images/products/sports-car.jpg',
```

#### 更新首页 (`src/app/page.tsx`)

找到所有 `placehold.co` 链接并替换为本地路径。

#### 更新博客页面 (`src/app/blog/page.tsx`)

同样替换博客图片路径。

## 📋 需要更新的文件清单

1. `src/app/page.tsx` - 首页图片
2. `src/app/products/page.tsx` - 产品图片
3. `src/app/about/page.tsx` - 关于页面图片
4. `src/app/blog/page.tsx` - 博客列表图片
5. `content/blog/*.md` - 博客文章中的图片引用

## 🎯 快速批量替换（可选）

如果需要批量替换，可以使用查找替换功能：

**查找**：`https://placehold.co/600x400?text=`
**替换**：`/images/products/`

然后手动修改具体的文件名。

## 🌐 优化建议

1. **图片格式**：使用 WebP 格式可以获得更好的压缩
2. **图片优化**：使用工具如 TinyPNG 压缩图片
3. **响应式图片**：使用 Next.js Image 组件自动优化

## 📤 部署

更新完成后：
```bash
git add .
git commit -m "Replace placeholder images with real product images"
git push origin main
```

Vercel 会自动重新部署。
